spring:
    datasource:
        username: postgres
        password: postgres
    flyway:
        enabled: true
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

# Embedded Postgres settings
embedded:
    postgres:
        database: postgres
        password: postgres
        username: postgres